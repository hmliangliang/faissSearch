## 与上一个版本的不同之处

1.上一版本采用list列表来保存结果,从实际运行结果来看,1300多万的查询向量(64维)，每次从20万候选向量中查询出相似度最高的top-100向量(64维)耗时2-3毫秒，但是每次把查询到top-100结果结果通过list.append()方法写入列表却耗时高达30-50毫秒，显然这样高的耗时无法接受，
通过分析得知,list每次append需要重新分配内存，耗时较大，因此采用numpy数组，一次性分配所需要的内存，大大提高了写入速度。通过优化，每次查询包含写入过程仅需1.2-1.5毫秒。
